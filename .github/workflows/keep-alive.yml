name: Keep dashboard awake
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
       - run: curl -sL --max-redirs 5 -o /dev/null https://tenora-fx-dashboard-8kctdwsnkqvljakjwgezvx.streamlit.app || true
